language: python

python:
  - 2.7
  - pypy

#before_install:
#  - sudo apt-get install python-tk

env:
  - DJANGO=1.8.6

install:
  - pip install -r requirements.txt

script: 
  - python setup.py test
